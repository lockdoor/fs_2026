# JavaScript Workshop: Arrays

This plan outlines the creation of a structured workshop in `array.js` to teach core JavaScript array concepts.

## Proposed Changes

### [New Workshop File]

#### [NEW] [array.js](file:///Users/pitsanunamnil/Desktop/workshop/fs_course_2026/array.js)

I will create this file with the following sections:
- **Part 1: What, Why, and How**
    - Basic array syntax and why we use them for ordered lists.
- **Part 2: Accessing and Building**
    - Indexing (starts at 0) and basic modification (`push`, `pop`, `shift`, `unshift`).
- **Part 3: Data Types in Arrays**
    - Storing mixed types including objects and nested arrays.
- **Part 4: Arrays vs. Objects**
    - When to use which (Ordered list vs. Named properties).
- **Part 5: Proving Arrays are Not Constants**
    - Demonstrate that `const` prevents reassignment but allows mutation.
- **Part 6: Looping through Arrays**
    - `for` loop, `forEach`, and `for...of`.
- **Part 7: Powerful Array Methods**
    - `map` (Transform), `filter` (Select), `find` (Search), `reduce` (Aggregate).
- **Part 8: Alignment with Shopping Cart**
    - Introduce `sort` and `splice` as needed for the final project.

## Verification Plan

### Manual Verification
- Run the code using `node array.js` to ensure all examples output correctly.
- Review comments for clarity and educational flow.
